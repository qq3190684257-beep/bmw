name: Build PoolTrajectoryHybrid arm64 dylib

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

permissions:
  contents: read

jobs:
  build:
    runs-on: macos-14
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Toolchain profile
        run: |
          xcodebuild -version
          xcrun --sdk iphoneos --show-sdk-version
          xcrun --sdk iphoneos --find clang++

      - name: Geometry tests and arm64 build
        run: make package

      - name: Verify artifact
        run: |
          file dist/PoolTrajectoryHybrid.dylib
          codesign -dvv dist/PoolTrajectoryHybrid.dylib
          shasum -a 256 -c dist/SHA256SUMS.txt

      - name: Upload dylib
        uses: actions/upload-artifact@v4
        with:
          name: PoolTrajectoryHybrid-arm64
          path: |
            dist/PoolTrajectoryHybrid.dylib
            dist/PoolTrajectoryHybrid-arm64.zip
            dist/SHA256SUMS.txt
            dist/README.md
            dist/RELEASE_NOTES_*.md
          if-no-files-found: error
          retention-days: 14
